<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="512" height="512" viewBox="0 0 512 512" role="img" aria-label="App icon">
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#0a84ff"/>
      <stop offset="1" stop-color="#0562c9"/>
    </linearGradient>
  </defs>
  <!-- Скруглённый фон -->
  <rect x="0" y="0" width="512" height="512" rx="64" fill="url(#g)"/>

  <!-- Лёгкий акцентный круг -->
  <circle cx="256" cy="256" r="140" fill="#ffffff" opacity=".1"/>

  <!-- Монограмма (замени букву при желании, например на N) -->
  <text x="50%" y="56%" text-anchor="middle" dominant-baseline="middle"
        font-family="Segoe UI, Arial, Helvetica, sans-serif"
        font-size="240" font-weight="700" fill="#ffffff">A</text>
</svg>
